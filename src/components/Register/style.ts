import styled from "styled-components";
export const RegisterStyle = styled.main`
  div {
    text-align: center;
  }
`;

export const SectionMiniCard = styled.section`
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px,1fr));
  gap: 2rem;
  margin: 3rem 0;
`;
